pipeline {
    agent any

    stages {

        stage('Checkout') {
            steps {
                git branch: 'main',
                    url: 'https://github.com/devsecopsmahesh-hue/sample-mavenproject.git'
            }
        }
    }
}